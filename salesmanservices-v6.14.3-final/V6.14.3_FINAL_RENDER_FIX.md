# V6.14.3 — Final sold-grid render fix

- Recently Sold layout is enforced at runtime with inline `!important` styles from JavaScript: 3 columns desktop, 2 tablet, 1 mobile.
- This does not depend on stylesheet cascade order, so generic account-grid CSS cannot override it.
- Delivery wording is now consistent: eligible account purchases are automatically emailed after payment confirmation; service orders remain Discord-based.
- Resend messages use `ADMIN_EMAIL` as Reply-To when configured.
- Asset/version cache keys bumped to 6.14.3.
