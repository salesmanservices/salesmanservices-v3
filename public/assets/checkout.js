const params=new URLSearchParams(location.search);const accountId=params.get('account');let poll;let countdownTimer;let orderToken='';
function statusLabel(d){if(d.deliveryStatus==='sent')return'✓ Payment confirmed — your account details have been delivered by email.';if(d.deliveryStatus==='failed')return'Payment confirmed — email delivery is retrying or needs support attention.';if(d.paymentStatus==='confirmed')return'✓ Payment confirmed — preparing automatic email delivery.';if(d.paymentStatus==='detected')return`Payment detected (${d.confirmations||0}/1 confirmation). Waiting for confirmation.`;if(d.paymentStatus==='expired')return'Order expired. The account reservation was released.';return`Waiting for payment… (${d.confirmations||0}/1 confirmation)`}
function setStep(step){document.querySelectorAll('.checkout-steps li').forEach((el,i)=>{el.classList.toggle('active',i===step-1);el.classList.toggle('done',i<step-1)})}
async function refreshOrder(id){const q=orderToken?`?token=${encodeURIComponent(orderToken)}`:'';try{const r=await fetch('/api/checkout/order/'+encodeURIComponent(id)+q,{cache:'no-store'});const d=await r.json();const status=document.querySelector('#paymentStatus');if(!status)return;if(!r.ok){status.textContent=d.error||'Could not refresh order status.';return}status.textContent=statusLabel(d);if(d.paymentStatus==='detected'||d.paymentStatus==='confirmed')setStep(3);if(d.deliveryStatus==='sent'){setStep(3);status.classList.add('paid')}if(d.status==='expired'){clearInterval(poll);setStep(3)} }catch{const status=document.querySelector('#paymentStatus');if(status)status.textContent='Connection temporarily unavailable — we will keep checking automatically.'}}
async function copyText(value,button){try{await navigator.clipboard.writeText(value);const old=button.textContent;button.textContent='✓ Copied';button.classList.add('copied');setTimeout(()=>{button.textContent=old;button.classList.remove('copied')},1600)}catch{prompt('Copy this value:',value)}}
function startCountdown(expiresAt){clearInterval(countdownTimer);const el=document.querySelector('#expiresCountdown');const update=()=>{if(!el)return;const ms=Math.max(0,Date.parse(expiresAt)-Date.now());const total=Math.floor(ms/1000);const m=String(Math.floor(total/60)).padStart(2,'0');const s=String(total%60).padStart(2,'0');el.textContent=`${m}:${s}`;if(ms<=0){clearInterval(countdownTimer);el.textContent='Expired'}};update();countdownTimer=setInterval(update,1000)}
const fallbackPaymentMethods=[{id:'btc',name:'Bitcoin',icon:'₿',enabled:true,recommended:true,mode:'automatic'},{id:'ltc',name:'Litecoin',icon:'Ł',enabled:true,recommended:false,mode:'automatic'}];
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]))}
function normalizePaymentMethods(value){if(!Array.isArray(value))return fallbackPaymentMethods;const methods=value.filter(m=>m&&m.enabled!==false&&m.id&&m.name).map(m=>({id:String(m.id),name:String(m.name),icon:String(m.icon||'💳'),recommended:Boolean(m.recommended),mode:m.mode==='manual'?'manual':'automatic'}));return methods.length?methods:fallbackPaymentMethods}
function renderPaymentMethods(methods,usedFallback=false){const holder=document.querySelector('#paymentMethods');if(!holder)return;holder.innerHTML=methods.map((m,i)=>`<label class="coin"><input type="radio" name="payment" value="${escapeHtml(m.id)}" ${i===0?'checked':''}><span>${escapeHtml(m.icon)} ${escapeHtml(m.name)}${m.recommended?' · Recommended':''}<small>${m.mode==='automatic'?'Automatic confirmation':'Manual confirmation'}</small></span></label>`).join('')+(usedFallback?'<small class="payment-fallback-note">Bitcoin and Litecoin are available. Checkout remains operational while custom payment settings are refreshed.</small>':'');document.querySelector('#submit').disabled=false}
async function loadProduct(){const el=document.querySelector('#product');if(!accountId){el.textContent='No account selected.';document.querySelector('#checkoutForm').hidden=true;return}try{const r=await fetch('/api/site-data',{cache:'no-store'});if(!r.ok)throw new Error(`Site data request failed (${r.status})`);const d=await r.json();const a=(Array.isArray(d.accounts)?d.accounts:[]).find(x=>String(x.id)===String(accountId));if(!a||a.status!=='available'){el.innerHTML='<strong>This account is unavailable.</strong><small class="field-help">Please return to the live inventory and choose another account.</small>';document.querySelector('#checkoutForm').hidden=true;return}if(a.saleMethod==='discord'){location.href=d.settings?.discord||'https://discord.gg/xDSvKT3ThQ';return}el.innerHTML=`<strong>${escapeHtml(a.title)}</strong><span>$${Number(a.price||0).toFixed(2)} USD</span>`;renderPaymentMethods(normalizePaymentMethods(d.paymentMethods),!Array.isArray(d.paymentMethods)||!d.paymentMethods.length)}catch(error){console.error('Checkout data load failed:',error);if(el.textContent==='Loading account…')el.textContent='Account selected. Payment options are available below.';renderPaymentMethods(fallbackPaymentMethods,true)}}
renderPaymentMethods(fallbackPaymentMethods);loadProduct();
document.querySelector('#checkoutForm').addEventListener('submit',async e=>{e.preventDefault();const btn=document.querySelector('#submit');const email=document.querySelector('#email').value.trim();if(!email){document.querySelector('#email').focus();return}btn.disabled=true;btn.innerHTML='<span>Creating secure order…</span><span class="button-arrow">⏳</span>';const selectedPayment=document.querySelector('input[name=payment]:checked');if(!selectedPayment){alert('Choose a payment method.');btn.disabled=false;btn.innerHTML='<span>Create order & reserve account</span><span class="button-arrow">→</span>';return}try{const body={accountId,email,discord:document.querySelector('#discord').value.trim(),paymentMethod:selectedPayment.value};const r=await fetch('/api/checkout/create',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok){alert(d.error||'Could not create order');return}orderToken=d.publicToken||'';document.querySelector('#checkoutForm').hidden=true;setStep(2);const result=document.querySelector('#result');result.hidden=false;const statusUrl=d.statusUrl||`/order?order=${encodeURIComponent(d.orderId)}&token=${encodeURIComponent(orderToken)}`;const automatic=d.paymentMode==='automatic';const amount=automatic?`${Number(d.cryptoAmount).toFixed(8)} ${escapeHtml(d.paymentMethod)}`:`$${Number(d.uniqueAmountUsd).toFixed(2)} USD`;const paymentPanel=automatic?`<p class="payment-instruction">Send the <b>exact amount</b> below to the address shown.</p><div class="copy-row"><div class="crypto-amount">${amount}</div><button class="copy-button" type="button" data-copy="${amount}">Copy amount</button></div><div class="copy-row"><div class="wallet">${escapeHtml(d.paymentAddress||'')}</div><button class="copy-button" type="button" data-copy="${escapeHtml(d.paymentAddress||'')}">Copy address</button></div><div class="exact-warning"><strong>⚠ Important</strong>Send the exact amount shown. A different amount can delay automatic detection.</div>`:`<div class="delivery-confirm"><strong>${escapeHtml(d.paymentMethodName||d.paymentMethod)}</strong><p>${escapeHtml(d.paymentInstructions||'Follow the payment details below, then contact support for confirmation.')}</p></div><div class="copy-row"><div class="wallet">${escapeHtml(d.paymentAddress||'Contact Discord for payment details')}</div>${d.paymentAddress?`<button class="copy-button" type="button" data-copy="${escapeHtml(d.paymentAddress)}">Copy details</button>`:''}</div><p>Amount due: <b>${amount}</b></p>`;result.innerHTML=`<div class="order-box"><h2 class="success-title">Order reserved successfully</h2><p class="lead">Keep this page open or save the live status link below.</p><div class="copy-row"><div class="order-id-value">${escapeHtml(d.orderId)}</div><button class="copy-button" type="button" data-copy="${escapeHtml(d.orderId)}">Copy order ID</button></div><div class="success-checks"><span>✓ Account reserved</span><span>✓ ${automatic?'Send payment to the address below':'Payment instructions shown below'}</span><span>✓ Account details will be emailed after payment confirmation</span></div><div class="expires-panel"><span>Reservation expires in</span><b id="expiresCountdown">30:00</b></div>${paymentPanel}<p id="paymentStatus">${automatic?'Waiting for payment… (0/1 confirmation)':'Waiting for manual confirmation…'}</p><p><a class="status-link" href="${statusUrl}">Open live order status →</a></p><p class="field-help">You can safely close this page and return using the status link.</p></div>`;result.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',()=>copyText(b.dataset.copy,b)));history.replaceState(null,'',statusUrl);startCountdown(d.expiresAt);refreshOrder(d.orderId);poll=setInterval(()=>refreshOrder(d.orderId),15000)}catch(error){console.error(error);alert('We could not create the order. Please try again or contact Discord support.')}finally{btn.disabled=false}});
