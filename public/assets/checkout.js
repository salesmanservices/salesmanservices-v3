const params=new URLSearchParams(location.search);const accountId=params.get('account');let poll;let countdownTimer;let orderToken='';
function statusLabel(d){if(d.deliveryStatus==='sent')return'Credentials delivered by email.';if(d.deliveryStatus==='failed')return'Payment confirmed, but email delivery needs admin attention.';if(d.paymentStatus==='confirmed')return'Payment confirmed — preparing automatic email delivery.';if(d.paymentStatus==='detected')return`Payment detected (${d.confirmations||0}/1 confirmation).`;if(d.paymentStatus==='expired')return'Order expired. The account reservation was released.';return`Waiting for payment… (${d.confirmations||0}/1 confirmation)`}
async function refreshOrder(id){const q=orderToken?`?token=${encodeURIComponent(orderToken)}`:'';const r=await fetch('/api/checkout/order/'+encodeURIComponent(id)+q);const d=await r.json();const status=document.querySelector('#paymentStatus');if(!status)return;if(!r.ok){status.textContent=d.error||'Could not refresh order status.';return}status.textContent=statusLabel(d);if(['paid','completed','expired'].includes(d.status)){clearInterval(poll);if(d.deliveryStatus==='sent')status.classList.add('paid')}}
async function copyText(value,button){try{await navigator.clipboard.writeText(value);const old=button.textContent;button.textContent='✓ Copied';button.classList.add('copied');setTimeout(()=>{button.textContent=old;button.classList.remove('copied')},1600)}catch{prompt('Copy this value:',value)}}
function startCountdown(expiresAt){clearInterval(countdownTimer);const el=document.querySelector('#expiresCountdown');const update=()=>{if(!el)return;const ms=Math.max(0,Date.parse(expiresAt)-Date.now());const total=Math.floor(ms/1000);const m=String(Math.floor(total/60)).padStart(2,'0');const s=String(total%60).padStart(2,'0');el.textContent=`${m}:${s}`;if(ms<=0){clearInterval(countdownTimer);el.textContent='Expired'}};update();countdownTimer=setInterval(update,1000)}
async function loadProduct(){const el=document.querySelector('#product');if(!accountId){el.textContent='No account selected.';document.querySelector('#checkoutForm').hidden=true;return}try{const r=await fetch('/api/site-data');const d=await r.json();const a=(d.accounts||[]).find(x=>String(x.id)===String(accountId));if(!a||a.status!=='available'){el.textContent='This account is unavailable.';document.querySelector('#checkoutForm').hidden=true;return}if(a.saleMethod==='discord'){location.href=d.settings?.discord||'https://discord.gg/xDSvKT3ThQ';return}el.innerHTML=`<strong>${a.title}</strong><span>$${Number(a.price||0).toFixed(2)} USD</span>`}catch{el.textContent='Could not load the account.'}}
loadProduct();
document.querySelector('#checkoutForm').addEventListener('submit',async e=>{e.preventDefault();const btn=document.querySelector('#submit');btn.disabled=true;btn.textContent='Creating order…';const body={accountId,email:document.querySelector('#email').value,discord:document.querySelector('#discord').value,paymentMethod:document.querySelector('input[name=payment]:checked').value};const r=await fetch('/api/checkout/create',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();btn.disabled=false;btn.textContent='Create order & reserve account';if(!r.ok){alert(d.error||'Could not create order');return}orderToken=d.publicToken||'';document.querySelector('#checkoutForm').hidden=true;const result=document.querySelector('#result');result.hidden=false;const statusUrl=d.statusUrl||`/order?order=${encodeURIComponent(d.orderId)}&token=${encodeURIComponent(orderToken)}`;const amount=`${Number(d.cryptoAmount).toFixed(8)} ${d.paymentMethod}`;result.innerHTML=`<div class="order-box"><h2 class="success-title">🎉 Order Created!</h2><div class="copy-row"><div class="order-id-value">${d.orderId}</div><button class="copy-button" type="button" data-copy="${d.orderId}">Copy order ID</button></div><div class="success-checks"><span>✓ Account reserved</span><span>✓ Waiting for blockchain payment</span><span>✓ After 1 confirmation, your account credentials are emailed automatically</span></div><div class="expires-panel"><span>Order expires in</span><b id="expiresCountdown">30:00</b></div><div class="delivery-confirm"><strong>What happens after payment?</strong><p>After one blockchain confirmation, the account username, password, registered email, email password, recovery information and delivery notes will be emailed automatically to the address you entered.</p></div><p>Send the <b>exact amount</b> below:</p><div class="copy-row"><div class="crypto-amount">${amount}</div><button class="copy-button" type="button" data-copy="${amount}">Copy amount</button></div><div class="copy-row"><div class="wallet">${d.paymentAddress}</div><button class="copy-button" type="button" data-copy="${d.paymentAddress}">Copy address</button></div><div class="exact-warning"><strong>⚠ Important</strong>Send the exact amount shown. Sending a different amount may delay automatic payment detection.</div><p>Invoice value: <b>$${Number(d.uniqueAmountUsd).toFixed(2)} USD</b>. The small unique cents identify your order automatically.</p><p id="paymentStatus">Waiting for payment… (0/1 confirmation)</p><p><a class="status-link" href="${statusUrl}">Open your live order status page</a></p><p>Save the order-status link. You may close checkout and return to it later.</p></div>`;result.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',()=>copyText(b.dataset.copy,b)));history.replaceState(null,'',statusUrl);startCountdown(d.expiresAt);refreshOrder(d.orderId);poll=setInterval(()=>refreshOrder(d.orderId),15000);});
