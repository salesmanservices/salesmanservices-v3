let data={accounts:[],reviews:[],announcement:{enabled:false,text:""},settings:{}};const panel=document.querySelector('#panel'),statusEl=document.querySelector('#status');
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function load(){try{const r=await fetch('/api/admin/data');if(!r.ok)throw new Error((await r.json()).error||'Access denied');data=await r.json();data.accounts ||= [];data.reviews ||= [];render('dashboard');statusEl.textContent='Connected'}catch(e){panel.innerHTML=`<div class="notice"><b>Admin backend is not connected yet.</b><p>${esc(e.message)}</p><p>Complete CLOUDFLARE_SETUP/START_HERE.txt. The public website remains fully operational.</p></div>`;statusEl.textContent='Setup required'}}
function render(tab){document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));if(tab==='dashboard')panel.innerHTML=`<div class="toolbar"><div><h1>Overview</h1><p class="muted">Manage the live website without changing its design.</p></div></div><div class="cards"><div class="card">Available accounts<b>${data.accounts.filter(a=>a.status!=='sold').length}</b></div><div class="card">Recently sold<b>${data.accounts.filter(a=>a.status==='sold').length}</b></div><div class="card">Reviews<b>${data.reviews.length}</b></div><div class="card">Languages<b>3</b></div></div>`;
if(tab==='accounts')renderAccounts();if(tab==='reviews')renderReviews();if(tab==='content')renderContent();if(tab==='export')renderExport()}
function renderAccounts(){panel.innerHTML=`<div class="toolbar"><div><h1>Accounts</h1><p class="muted">Add, edit, remove or mark accounts sold.</p></div><button class="action" id="addAccount">+ Add account</button></div><div class="list">${data.accounts.map((a,i)=>`<div class="row"><img src="/${esc(a.image)}"><div><b>${esc(a.title)}</b><div class="muted">${esc(a.type)}</div></div><input data-i="${i}" data-k="price" type="number" value="${esc(a.price)}"><select data-i="${i}" data-k="status"><option value="available" ${a.status!=='sold'?'selected':''}>Available</option><option value="sold" ${a.status==='sold'?'selected':''}>Sold</option></select><button class="action danger" data-delete="${i}">Delete</button></div>`).join('')}</div>`;document.querySelector('#addAccount').onclick=()=>{data.accounts.unshift({id:'account-'+Date.now(),type:'Account',title:'New OSRS Account',price:0,image:'assets/accounts/zerker-1.png',tags:['Negotiable'],description:'Edit this description.',buy:data.settings.discord||'https://discord.gg/HkUCNNQtmG',button:'Reserve on Discord',status:'available'});renderAccounts()};bindInputs();document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>{if(confirm('Delete this account?')){data.accounts.splice(+b.dataset.delete,1);renderAccounts()}});document.querySelectorAll('.row div:nth-child(2)').forEach((el,i)=>el.onclick=()=>editAccount(i))}
function editAccount(i){const a=data.accounts[i];panel.innerHTML=`<div class="toolbar"><h1>Edit account</h1><button class="action" id="back">← Back</button></div><div class="editor grid2">${['title','type','price','image','secondImage','description','buy','button','soldDate'].map(k=>`<label>${k}<${k==='description'?'textarea':'input'} data-edit="${k}" ${k==='price'?'type="number"':''}>${k==='description'?esc(a[k]||''):''}</${k==='description'?'textarea':'input'}></label>`).join('')}<label>Tags (comma separated)<input id="tags" value="${esc((a.tags||[]).join(', '))}"></label><label>Status<select id="editStatus"><option value="available">Available</option><option value="sold">Sold</option></select></label></div>`;document.querySelectorAll('input[data-edit]').forEach(x=>x.value=a[x.dataset.edit]??'');document.querySelector('#editStatus').value=a.status||'available';document.querySelector('#back').onclick=()=>{document.querySelectorAll('[data-edit]').forEach(x=>a[x.dataset.edit]=x.dataset.edit==='price'?Number(x.value):x.value);a.tags=document.querySelector('#tags').value.split(',').map(x=>x.trim()).filter(Boolean);a.status=document.querySelector('#editStatus').value;renderAccounts()}}
function renderReviews(){panel.innerHTML=`<div class="toolbar"><div><h1>Reviews</h1><p class="muted">Saved for future dynamic review cards and AI knowledge.</p></div><button class="action" id="addReview">+ Add review</button></div><div class="list">${data.reviews.map((r,i)=>`<div class="editor"><label>Name<input data-r="${i}" data-k="name" value="${esc(r.name)}"></label><label>Source<input data-r="${i}" data-k="source" value="${esc(r.source)}"></label><label>Feedback<textarea data-r="${i}" data-k="text">${esc(r.text)}</textarea></label><button class="action danger" data-rdelete="${i}">Delete</button></div>`).join('')}</div>`;document.querySelector('#addReview').onclick=()=>{data.reviews.unshift({name:'Customer',source:'Discord',text:'Great service.'});renderReviews()};document.querySelectorAll('[data-r]').forEach(x=>x.oninput=()=>data.reviews[+x.dataset.r][x.dataset.k]=x.value);document.querySelectorAll('[data-rdelete]').forEach(x=>x.onclick=()=>{data.reviews.splice(+x.dataset.rdelete,1);renderReviews()})}
function renderContent(){panel.innerHTML=`<h1>Content & links</h1><div class="editor"><label><input id="annEnabled" type="checkbox" ${data.announcement?.enabled?'checked':''}> Enable website announcement</label><label>Announcement<textarea id="annText">${esc(data.announcement?.text||'')}</textarea></label><label>Discord link<input id="discord" value="${esc(data.settings?.discord||'')}"></label><label>SellApp link<input id="sellapp" value="${esc(data.settings?.sellapp||'')}"></label></div>`;['annEnabled','annText','discord','sellapp'].forEach(id=>document.querySelector('#'+id).oninput=()=>{data.announcement={enabled:document.querySelector('#annEnabled').checked,text:document.querySelector('#annText').value};data.settings={...data.settings,discord:document.querySelector('#discord').value,sellapp:document.querySelector('#sellapp').value}})}
function renderExport(){panel.innerHTML=`<h1>Backup & export</h1><div class="editor"><p>Download a full JSON backup before major updates.</p><button class="action" id="download">Download backup</button><label style="margin-top:20px">Restore backup<input id="restore" type="file" accept="application/json"></label></div>`;document.querySelector('#download').onclick=()=>{const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'}));a.download='salesman-site-backup.json';a.click()};document.querySelector('#restore').onchange=async e=>{data=JSON.parse(await e.target.files[0].text());render('dashboard')}}
function bindInputs(){document.querySelectorAll('[data-i][data-k]').forEach(x=>x.oninput=()=>data.accounts[+x.dataset.i][x.dataset.k]=x.dataset.k==='price'?Number(x.value):x.value)}
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>render(b.dataset.tab));document.querySelector('#save').onclick=async()=>{statusEl.textContent='Saving…';const r=await fetch('/api/admin/data',{method:'PUT',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const out=await r.json();statusEl.textContent=r.ok?'Saved '+new Date().toLocaleTimeString():out.error||'Save failed'};load();